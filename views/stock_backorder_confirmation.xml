<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="stock.view_backorder_confirmation" model="ir.ui.view">
        <field name="name">stock_backorder_confirmation</field>
        <field name="model">stock.backorder.confirmation</field>
        <field name="arch" type="xml">
            <form string="Backorder creation">
                <group>
                    <p>
                        It seems that ordered quantity is miss matched with received quantity.
                    
                        <b>Do you want To Process further ?</b>
                    </p>
                </group>
                <footer>
                    <button name="process" string="Process with BackOrder" type="object" class="oe_highlight"/>
                    <button name="process_cancel_backorder" string="No Backorder" type="object" class="btn-primary"/>
                    <button string="_Cancel" class="btn-default" special="cancel" />
                </footer>
            </form>
        </field>
    </record>
</odoo>
